[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
aws-cdk-lib = ">=2.100.0"
constructs = ">=10.0.0"

[dev-packages]
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
hypothesis = ">=6.0.0"
black = "==26.1.0"
ruff = ">=0.1.0"
mypy = ">=1.0.0"

[requires]
python_version = "3.11"
