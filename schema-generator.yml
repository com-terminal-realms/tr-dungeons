project:
  name: tr-dungeons
  customerId: terminal-realms
  projectId: tr-dungeons

paths:
  schemas: ./schemas

output:
  python:
    targets:
      api:
        path: ./apps/api/models
        enabled: true
  
  typescript:
    targets:
      api:
        path: ./apps/web/src/models
        enabled: false
  
  cdk:
    targets:
      api:
        path: ./infrastructure/cdk/resources
        enabled: true
