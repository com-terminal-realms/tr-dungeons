version: '1.0'

project:
  name: tr-dungeons
  customerId: terminal-realms
  projectId: tr-dungeons

paths:
  schemas: ./schemas

output:
  backend:
    models: ./apps/api/models
    enabled: true
  infrastructure:
    cdk:
      output: ./infrastructure/cdk/resources
      enabled: true

options:
  python:
    base_class: pydantic.BaseModel
    generate_validators: true
