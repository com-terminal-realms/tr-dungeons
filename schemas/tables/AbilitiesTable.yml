type: table
name: AbilitiesTable
description: DynamoDB table for ability definitions
targets:
  - api
model:
  name: Ability
  attributes: {}
keys:
  partition_key:
    name: pk
    type: string
  sort_key:
    name: sk
    type: string
indexes:
  - name: ability-type-index
    partition_key:
      name: ability_type
      type: string
    projection: all
