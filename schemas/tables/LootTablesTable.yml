type: table
name: LootTablesTable
description: DynamoDB table for loot table configurations
targets:
  - api
model:
  name: LootTable
  attributes: {}
keys:
  partition_key:
    name: pk
    type: string
  sort_key:
    name: sk
    type: string
indexes:
  - name: name-index
    partition_key:
      name: name
      type: string
    projection: all
